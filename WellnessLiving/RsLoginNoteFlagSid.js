/**
 * List of modes to change user's "flag" status within a location
 */
function RsLoginNoteFlagSid()
{
  // Empty constructor.
}

/**
 * Add flag record.
 *
 * @type {number}
 */
RsLoginNoteFlagSid.ADD = 1;

/**
 * Remove flag record.
 *
 * @type {number}
 */
RsLoginNoteFlagSid.REMOVE = 2;
